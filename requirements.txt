# ============================================================
# Python 插件依赖
# ============================================================
# 主服务是 Node.js，Python 只用于插件子进程。
# 每个插件可以用自己的 requirements.txt，
# 这里放所有插件的公共依赖。

# HTTP 请求 (大部分插件都需要)
httpx>=0.27.0
requests>=2.32.0

# AI/LLM 库 (生图、生视频插件用)
openai>=1.50.0
google-genai>=1.0.0

# 图像处理
Pillow>=10.0.0

# 通用工具
pyyaml>=6.0
python-dateutil>=2.9.0
